# ⚡ Быстрый деплой в продакшн

## 📝 Чеклист перед деплоем

- [ ] Код протестирован локально
- [ ] WhatsApp уведомления работают
- [ ] Все изменения закоммичены в Git

## 🔑 Переменные окружения для ps.kz

Скопируйте и настройте в панели управления ps.kz:

```env
# Обязательные
DATABASE_URL=postgresql://user:password@host:5432/database
AUTH_SECRET=<сгенерируйте новый: openssl rand -base64 32>
NODE_ENV=production

# WhatsApp (если используете)
WHATSAPP_ENABLED=true
WHATSAPP_API_URL=https://api.green-api.com
WHATSAPP_API_KEY=idInstance:apiTokenInstance
WHATSAPP_GROUP_ID=your-group-id
```

## 🚀 Шаги деплоя

1. **Соберите проект:**
   ```bash
   npm run build
   ```

2. **Загрузите на ps.kz:**
   - Через Git (если настроен)
   - Или через FTP/SFTP (папки: `.next/`, `node_modules/`, `prisma/`, `public/`, `src/`, файлы конфигурации)

3. **На сервере:**
   ```bash
   npm install --production
   npx prisma generate
   npx prisma db push
   ```

4. **Настройте переменные окружения** в панели ps.kz

5. **Запустите приложение** в панели ps.kz

6. **Проверьте:**
   - Сайт открывается
   - Вход работает
   - Создание заявки работает
   - WhatsApp уведомление приходит

## ✅ Готово!

Подробная инструкция: [DEPLOYMENT_STEPS.md](./DEPLOYMENT_STEPS.md)
