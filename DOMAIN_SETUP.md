# 🌐 Настройка домена для Telfera.kz

Пошаговая инструкция по подключению вашего домена к приложению на ps.kz

## 📋 Подготовка

Перед началом убедитесь, что:
- [ ] У вас есть доступ к панели управления доменом (где вы покупали домен)
- [ ] У вас есть доступ к панели управления ps.kz
- [ ] Приложение уже развернуто на ps.kz и работает

## 🚀 Шаг 1: Добавление домена в панели ps.kz

1. Войдите в панель управления ps.kz
2. Найдите ваш проект/приложение
3. Перейдите в раздел **"Домены"** или **"Domains"**
4. Нажмите **"Добавить домен"** или **"Add Domain"**
5. Введите ваш домен (например: `telfera.kz` или `www.telfera.kz`)
6. Сохраните изменения

**Важно:** ps.kz покажет вам IP-адрес или CNAME запись, которую нужно будет настроить у регистратора домена.

## 🔧 Шаг 2: Настройка DNS записей у регистратора домена

Войдите в панель управления вашим доменом (где вы его регистрировали) и настройте DNS записи.

### Вариант A: Использование A-записи (IP-адрес)

Если ps.kz предоставил IP-адрес:

1. Найдите раздел **"DNS записи"** или **"DNS Management"**
2. Добавьте/измените A-запись:
   ```
   Тип: A
   Имя: @ (или оставьте пустым для основного домена)
   Значение: [IP-адрес от ps.kz]
   TTL: 3600 (или автоматически)
   ```

3. Если нужен поддомен `www`:
   ```
   Тип: A
   Имя: www
   Значение: [тот же IP-адрес от ps.kz]
   TTL: 3600
   ```

### Вариант B: Использование CNAME (рекомендуется)

Если ps.kz предоставил CNAME запись (например: `your-app.ps.kz`):

1. Найдите раздел **"DNS записи"** или **"DNS Management"**
2. Добавьте CNAME запись:
   ```
   Тип: CNAME
   Имя: @ (или www)
   Значение: [CNAME от ps.kz, например: your-app.ps.kz]
   TTL: 3600
   ```

**Примечание:** Некоторые регистраторы не позволяют использовать CNAME для корневого домена (@). В этом случае используйте A-запись.

## ⏱️ Шаг 3: Ожидание распространения DNS

После настройки DNS записей:

1. **Подождите 5-60 минут** (обычно 15-30 минут)
2. Проверьте распространение DNS с помощью онлайн-инструментов:
   - [whatsmydns.net](https://www.whatsmydns.net/)
   - [dnschecker.org](https://dnschecker.org/)
3. Введите ваш домен и проверьте, что DNS записи обновились по всему миру

**Важно:** Полное распространение DNS может занять до 48 часов, но обычно происходит быстрее.

## 🔒 Шаг 4: Настройка SSL/HTTPS

После того, как DNS записи обновились:

1. Вернитесь в панель управления ps.kz
2. Найдите раздел **"SSL"** или **"HTTPS"**
3. Включите SSL сертификат для вашего домена
4. ps.kz обычно предоставляет бесплатный SSL (Let's Encrypt)
5. Дождитесь активации SSL (обычно 5-15 минут)

**Проверка:** После активации ваш сайт должен открываться по `https://your-domain.com`

## 🔄 Шаг 5: Настройка редиректа (опционально)

Рекомендуется настроить редирект:

- `http://` → `https://` (принудительный HTTPS)
- `www.telfera.kz` → `telfera.kz` (или наоборот)

Обычно это настраивается в панели ps.kz в разделе настроек домена.

## ✅ Шаг 6: Проверка работы

Проверьте, что все работает:

1. **Откройте ваш домен в браузере:**
   ```
   https://telfera.kz
   ```

2. **Проверьте HTTPS:**
   - В адресной строке должен быть замочек 🔒
   - URL должен начинаться с `https://`

3. **Проверьте работу сайта:**
   - Главная страница загружается
   - Форма заявки работает
   - Вход в админ-панель работает

4. **Проверьте все страницы:**
   - `/` - главная
   - `/catalog/sha8` - каталог SHA8
   - `/catalog/balkans` - каталог Balkans
   - `/login` - страница входа
   - `/admin` - админ-панель (после входа)

## 🐛 Решение проблем

### Проблема: "DNS не обновился"

**Решение:**
- Подождите еще 30-60 минут
- Проверьте правильность DNS записей
- Убедитесь, что TTL не слишком большой
- Попробуйте очистить DNS кэш: `ipconfig /flushdns` (Windows) или `sudo dscacheutil -flushcache` (Mac)

### Проблема: "SSL сертификат не активируется"

**Решение:**
- Убедитесь, что DNS записи полностью обновились
- Проверьте, что домен правильно добавлен в ps.kz
- Подождите еще 15-30 минут
- Обратитесь в поддержку ps.kz

### Проблема: "Сайт открывается, но показывает ошибку"

**Решение:**
- Проверьте переменные окружения в ps.kz
- Проверьте логи приложения
- Убедитесь, что приложение запущено
- Проверьте, что порт настроен правильно

### Проблема: "Сайт работает по IP, но не по домену"

**Решение:**
- Проверьте DNS записи еще раз
- Убедитесь, что домен добавлен в панели ps.kz
- Проверьте, что нет конфликтующих DNS записей
- Подождите полного распространения DNS (до 48 часов)

## 📝 Дополнительные настройки

### Настройка переменных окружения (если нужно)

Обычно Next.js автоматически определяет домен, но если нужно явно указать:

```env
# В панели ps.kz добавьте (если требуется):
NEXT_PUBLIC_APP_URL=https://telfera.kz
```

**Примечание:** В текущей версии проекта это не требуется, так как домен определяется автоматически.

### Настройка email (если планируете)

Если в будущем планируете отправлять email уведомления, настройте:

1. SPF запись в DNS
2. DKIM запись в DNS
3. DMARC политику

Но это не обязательно для текущей версии проекта.

## ✅ Финальный чеклист

- [ ] Домен добавлен в панели ps.kz
- [ ] DNS записи настроены у регистратора
- [ ] DNS записи обновились (проверено через whatsmydns.net)
- [ ] SSL/HTTPS активирован и работает
- [ ] Сайт открывается по домену
- [ ] Все страницы работают корректно
- [ ] HTTPS принудительно включен
- [ ] Редирект www настроен (если нужно)

## 🎉 Готово!

Ваш домен подключен и работает! Теперь пользователи могут заходить на сайт по адресу `https://telfera.kz`

## 📞 Нужна помощь?

Если возникли проблемы:
1. Проверьте логи в панели ps.kz
2. Обратитесь в поддержку ps.kz
3. Проверьте документацию ps.kz по настройке доменов
