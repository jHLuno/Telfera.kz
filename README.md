# Telfera.kz

Современный веб-сайт для продажи телферов SHA8 и Balkans с системой управления лидами.

## 🚀 Возможности

- **Лендинг страница** — Современный дизайн с Bento-сеткой и анимациями
- **Форма захвата лидов** — Сбор заявок с сайта
- **Панель менеджера** — Просмотр и управление статусами лидов
- **Панель администратора** — Полное управление лидами и пользователями

## 🛠 Технологии

- **Next.js 15** — React фреймворк
- **TypeScript** — Типизация
- **Tailwind CSS** — Стилизация
- **Prisma** — ORM для базы данных
- **NextAuth.js** — Аутентификация
- **Framer Motion** — Анимации
- **Radix UI** — UI компоненты

## 📦 Установка

1. **Клонируйте репозиторий:**

```bash
git clone https://github.com/your-username/Telfera.kz.git
cd Telfera.kz
```

2. **Установите зависимости:**

```bash
npm install
```

3. **Создайте файл `.env`:**

```env
DATABASE_URL="file:./dev.db"
AUTH_SECRET="your-secret-key-generate-with-openssl-rand-base64-32"
```

4. **Инициализируйте базу данных:**

```bash
npx prisma db push
npx tsx prisma/seed.ts
```

5. **Запустите сервер разработки:**

```bash
npm run dev
```

Откройте [http://localhost:3000](http://localhost:3000) в браузере.

## 👤 Учетные данные

После запуска seed скрипта, будут созданы следующие пользователи:

| Роль | Email | Пароль |
|------|-------|--------|
| Администратор | admin@telfera.kz | admin123 |
| Менеджер | manager@telfera.kz | manager123 |

## 📁 Структура проекта

```
src/
├── app/                    # Next.js App Router
│   ├── admin/              # Панель администратора
│   ├── manager/            # Панель менеджера
│   ├── login/              # Страница входа
│   └── page.tsx            # Главная страница
├── components/             # React компоненты
│   └── ui/                 # UI компоненты
├── actions/                # Server Actions
├── lib/                    # Утилиты и конфиги
└── types/                  # TypeScript типы
```

## 🔐 Роли пользователей

- **ADMIN** — Полный доступ: управление лидами, пользователями, настройками
- **MANAGER** — Просмотр лидов и изменение их статусов

## 📝 Скрипты

- `npm run dev` — Запуск сервера разработки
- `npm run build` — Сборка для продакшена
- `npm run start` — Запуск продакшен сервера
- `npm run db:push` — Применение схемы к БД
- `npm run db:studio` — Открыть Prisma Studio
- `npm run db:seed` — Добавить начальные данные

## 🔒 Безопасность

Проект включает следующие меры безопасности:

- **Отключение кэша браузера** для защищенных страниц
- **Security headers** для защиты от XSS, clickjacking и других атак
- **Хеширование паролей** с помощью bcryptjs
- **Защита от SQL-инъекций** через Prisma ORM
- **Role-based access control** (ADMIN vs MANAGER)
- **Session-based authentication** с NextAuth.js

Подробнее смотрите в [SECURITY.md](./SECURITY.md)

## 🚀 Деплой на продакшен

Руководство по развертыванию на ps.kz смотрите в [DEPLOYMENT.md](./DEPLOYMENT.md)

**Важно для продакшена:**
- Смените `AUTH_SECRET` на случайную строку
- Измените пароль администратора после первого входа
- Используйте HTTPS
- Настройте регулярные бэкапы базы данных

## 📄 Лицензия

MIT
