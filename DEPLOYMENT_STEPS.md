# 🚀 Пошаговая инструкция для деплоя в продакшн

## Шаг 1: Подготовка кода

```bash
# Убедитесь, что все изменения закоммичены
git status

# Соберите проект для проверки
npm run build

# Проверьте, что сборка прошла успешно
npm run start
# Откройте http://localhost:3000 и проверьте работу
```

## Шаг 2: Подготовка переменных окружения

### Обязательные переменные:

1. **DATABASE_URL** - URL вашей PostgreSQL базы данных
   ```bash
   # Пример для Railway:
   DATABASE_URL=postgresql://user:password@host:5432/database
   ```

2. **AUTH_SECRET** - Секретный ключ для аутентификации
   ```bash
   # Сгенерируйте новый ключ:
   openssl rand -base64 32
   ```
   ⚠️ **ВАЖНО:** Используйте НОВЫЙ ключ для продакшена, не тот же что в разработке!

3. **NODE_ENV**
   ```env
   NODE_ENV=production
   ```

### WhatsApp уведомления (если используете):

```env
WHATSAPP_ENABLED=true
WHATSAPP_API_URL=https://api.green-api.com
WHATSAPP_API_KEY=idInstance:apiTokenInstance
WHATSAPP_GROUP_ID=your-group-id
```

## Шаг 3: Деплой на ps.kz

### Вариант A: Через Git (если настроен)

```bash
# Если у вас настроен Git на ps.kz
git push production main

# Затем на сервере:
ssh your-server
cd /path/to/project
npm install --production
npm run build
npm run start
```

### Вариант B: Через FTP/SFTP

1. **Соберите проект локально:**
   ```bash
   npm run build
   ```

2. **Загрузите на сервер через FTP/SFTP:**
   - `.next/` (папка со сборкой)
   - `node_modules/` (или установите на сервере через `npm install --production`)
   - `prisma/`
   - `public/`
   - `src/`
   - `package.json`
   - `package-lock.json`
   - `next.config.ts`
   - `tsconfig.json`

3. **На сервере установите зависимости:**
   ```bash
   npm install --production
   ```

## Шаг 4: Настройка базы данных

### Если используете Railway PostgreSQL:

1. Убедитесь, что база данных доступна с сервера ps.kz
2. Проверьте `DATABASE_URL` в переменных окружения

### Если используете PostgreSQL на ps.kz:

1. Создайте базу данных в панели управления ps.kz
2. Запишите данные подключения
3. Обновите `DATABASE_URL`

### Применение схемы базы данных:

```bash
# На сервере или локально (если есть доступ к продакшн БД)
npx prisma generate
npx prisma db push

# Опционально: создайте начальных пользователей
npx tsx prisma/seed.ts
```

## Шаг 5: Настройка переменных окружения на ps.kz

В панели управления ps.kz добавьте все переменные окружения:

1. Зайдите в настройки проекта/приложения
2. Найдите раздел "Environment Variables" или "Переменные окружения"
3. Добавьте все переменные из Шага 2

**Важно:** 
- Не добавляйте `.env` файл в Git!
- Используйте панель управления для переменных окружения

## Шаг 6: Настройка Node.js на ps.kz

В панели управления ps.kz:

- **Node Version:** 18.x или выше (рекомендуется 20.x)
- **Start Command:** `npm run start`
- **Build Command:** `npm run build` (если нужно)
- **Port:** Обычно настраивается автоматически, но проверьте

## Шаг 7: Запуск и проверка

1. **Запустите приложение** в панели управления ps.kz

2. **Проверьте работу:**
   - ✅ Откройте ваш сайт в браузере
   - ✅ Проверьте HTTPS (должен быть включен)
   - ✅ Попробуйте войти в систему
   - ✅ Создайте тестовую заявку
   - ✅ Проверьте, что заявка появилась в админ-панели
   - ✅ **Проверьте WhatsApp уведомление** (если настроено)

3. **Проверьте логи:**
   - В панели управления ps.kz найдите раздел "Logs"
   - Убедитесь, что нет ошибок

## Шаг 8: Настройка домена и SSL

1. **Настройте домен:**
   - В панели ps.kz добавьте ваш домен
   - Обновите DNS записи на вашем регистраторе домена
   - Подождите распространения DNS (до 48 часов)

2. **Включите SSL/HTTPS:**
   - Обычно ps.kz предоставляет бесплатный SSL
   - Включите в панели управления
   - Проверьте, что сайт открывается по HTTPS

**📖 Подробная инструкция:** Смотрите [DOMAIN_SETUP.md](./DOMAIN_SETUP.md) для детальных шагов по настройке домена

## Шаг 9: Безопасность

1. **Смените пароли:**
   - Войдите как администратор
   - Смените пароль администратора
   - Смените пароль менеджера (если нужно)

2. **Проверьте безопасность:**
   - Убедитесь, что HTTPS работает
   - Проверьте, что `.env` файл не доступен публично
   - Проверьте логи на подозрительную активность

## Шаг 10: Мониторинг

Настройте мониторинг:

- [ ] Uptime мониторинг (например, UptimeRobot)
- [ ] Мониторинг ошибок (логи в ps.kz)
- [ ] Регулярные бэкапы базы данных
- [ ] Мониторинг производительности

## 🔄 Обновление приложения

Когда нужно обновить код:

```bash
# 1. Локально соберите проект
npm run build

# 2. Загрузите обновленные файлы на сервер
# (особенно папку .next/ и измененные файлы)

# 3. На сервере (если нужно):
npm install --production

# 4. Перезапустите приложение в панели ps.kz
```

## 🐛 Решение проблем

### Проблема: "Cannot connect to database"
- Проверьте `DATABASE_URL` в переменных окружения
- Убедитесь, что база данных доступна с сервера
- Проверьте firewall правила

### Проблема: "WhatsApp notifications not working"
- Проверьте, что `WHATSAPP_ENABLED=true`
- Проверьте правильность `WHATSAPP_API_KEY` (формат: `idInstance:apiTokenInstance`)
- Проверьте `WHATSAPP_GROUP_ID`
- Проверьте логи на сервере для ошибок

### Проблема: "500 Internal Server Error"
- Проверьте логи в панели ps.kz
- Убедитесь, что все переменные окружения установлены
- Проверьте версию Node.js

### Проблема: "Module not found"
- Запустите `npm install --production` на сервере
- Проверьте, что `package.json` загружен
- Проверьте версию Node.js

## ✅ Финальный чеклист

- [ ] Сайт открывается и работает
- [ ] HTTPS включен и работает
- [ ] Вход/выход работает
- [ ] Создание заявок работает
- [ ] WhatsApp уведомления работают (если настроены)
- [ ] Админ-панель доступна
- [ ] Панель менеджера доступна
- [ ] База данных подключена
- [ ] Нет ошибок в консоли браузера
- [ ] Нет ошибок в логах сервера
- [ ] Все страницы загружаются быстро
- [ ] Сайт адаптивен на мобильных устройствах
- [ ] Пароли изменены с дефолтных

## 🎉 Готово!

Ваш сайт Telfera.kz теперь в продакшене! 🚀

Если возникнут вопросы, проверьте:
- Логи в панели управления ps.kz
- Документацию ps.kz
- Файл DEPLOYMENT.md для дополнительной информации
